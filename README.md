SmartDoc RAG is a Retrieval Augmented Generation (RAG) system that allows users to ask questions about documents and receive context-aware answers using Large Language Models.

The system processes documents, converts them into embeddings, stores them in a vector database, and retrieves relevant information to generate accurate responses.